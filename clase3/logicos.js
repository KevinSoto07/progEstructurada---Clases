let edad = 20;
let tieneDUI = true;
//let tieneDUI = false;

let puedeVotar = edad >=18 && tieneDUI ? "Puede votar" : "No puede votar";
console.log(puedeVotar);

//////////////////////////////////////////////////////////////////////

let edadPersona = 50;
let esMiembro = true;
let descuento = edadPersona >= 30 || esMiembro ? "Tiene descuento" : "No tiene descuento";
console.log(descuento);

//////////////////////////////////////////////////////////////////////

let persona = 14;
let altura = 1.60;
let puedeSubir = persona >= 12 && altura >= 1.60 ? "Puede subir" : "No puede subir";
console.log(puedeSubir);

//////////////////////////////////////////////////////////////////////

